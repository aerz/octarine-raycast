{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "octarine",
  "title": "Octarine",
  "description": "Control Octarine from Raycast",
  "icon": "icon.png",
  "author": "aerz",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Productivity"
  ],
  "preferences": [
    {
      "name": "workspaceRoots",
      "type": "textfield",
      "required": true,
      "title": "Workspace Root Paths",
      "description": "Comma-separated root folders to scan for Octarine workspaces",
      "placeholder": "~/Documents/Octarine, ~/Work/Notes"
    },
    {
      "name": "excludedWorkspaces",
      "type": "textfield",
      "placeholder": "folder1, folder2, ...",
      "title": "Exclude Workspaces",
      "required": false,
      "description": "Comma-separated workspace names to skip during workspace discovery"
    },
    {
      "name": "excludedFoldersInWorkspaces",
      "type": "textfield",
      "placeholder": "folder1, folder2, ...",
      "title": "Exclude Folders in Workspaces",
      "required": false,
      "description": "Comma-separated folder names to exclude from search results inside included workspaces"
    }
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "open-workspace",
      "title": "Open Workspace",
      "description": "Open a workspace in Octarine",
      "mode": "view",
      "arguments": [
        {
          "name": "workspace",
          "type": "text",
          "required": false,
          "placeholder": "Workspace name",
          "description": "Specify the workspace name to open it directly"
        }
      ]
    },
    {
      "name": "search-notes",
      "title": "Search Notes",
      "description": "Search notes across Octarine workspaces",
      "mode": "view",
      "preferences": [
        {
          "name": "showWorkspaceNoteCount",
          "type": "checkbox",
          "title": "Show Workspace Note Count",
          "description": "Show or hide note counts in workspace section titles",
          "label": "Show note count in workspace section titles",
          "default": false,
          "required": false
        },
        {
          "name": "showPinnedNotesFirst",
          "type": "checkbox",
          "title": "Show Pinned Notes First",
          "description": "Sort the pinned notes to the top of the list in each workspace.",
          "label": "Show pinned notes first",
          "default": false,
          "required": false
        },
        {
          "name": "searchContent",
          "type": "checkbox",
          "title": "Search Note Content",
          "description": "Search note text in addition to titles and paths",
          "label": "Include note content by default",
          "default": false,
          "required": false
        }
      ]
    },
    {
      "name": "search-attachments",
      "title": "Search Attachments",
      "description": "Browse all files attached to notes across your Octarine workspaces",
      "mode": "view",
      "preferences": [
        {
          "name": "showWorkspaceAttachmentCount",
          "type": "checkbox",
          "title": "Show Workspace Attachment",
          "description": "Display the number of attachments per workspace in parentheses",
          "label": "Show Workspace Attachment",
          "default": false,
          "required": false
        },
        {
          "name": "flattenWorkspaceSections",
          "type": "checkbox",
          "title": "Flatten workspace sections",
          "description": "Show attachments in one flat list without workspace grouping",
          "label": "Flatten workspace sections",
          "default": false,
          "required": false
        },
        {
          "name": "excludeFileExtensions",
          "type": "textfield",
          "title": "Exclude File Extensions",
          "description": "Comma-separated list of file extensions to omit from results.",
          "label": "Exclude File Extensions",
          "placeholder": "mp4, png, pdf",
          "required": false
        }
      ]
    },
    {
      "name": "open-daily-desk-note",
      "title": "Open Daily Desk Note",
      "description": "Browse or open Daily Desk notes by date",
      "mode": "view",
      "arguments": [
        {
          "name": "date",
          "type": "text",
          "required": false,
          "placeholder": "feb 3"
        },
        {
          "name": "workspace",
          "type": "text",
          "required": false,
          "placeholder": "Workspace name"
        }
      ],
      "preferences": [
        {
          "name": "defaultWorkspace",
          "type": "textfield",
          "title": "Default Workspace",
          "required": false,
          "description": "Set default workspace. If empty, a selector will be shown."
        },
        {
          "name": "showFilename",
          "type": "checkbox",
          "title": "Show Filename",
          "label": "Show filename (e.g. 2023-02-18) instead of natural dates",
          "required": false,
          "default": false,
          "description": "Display notes using their file name format instead of natural language dates"
        },
        {
          "name": "useLastWorkspace",
          "type": "checkbox",
          "title": "Use Last Workspace",
          "label": "Open suggested notes in the last used workspace",
          "required": false,
          "default": true,
          "description": "Remember the last workspace used and open suggested dates there instead of asking every time"
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.25",
    "@raycast/utils": "^2.3.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "@vitest/coverage-v8": "^5.0.1",
    "eslint": "^10.11.0",
    "prettier": "^3.9.8",
    "typescript": "~6.0.3",
    "vitest": "^5.0.1"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "ray publish"
  }
}
