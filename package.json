{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "octarine",
  "title": "Octarine",
  "description": "Control Octarine with Raycast",
  "icon": "extension-icon.png",
  "author": "aerz",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity"
  ],
  "preferences": [
    {
      "name": "workspaceRoots",
      "type": "textfield",
      "required": true,
      "title": "Workspace Root Paths",
      "description": "Comma-separated root folders to scan for Octarine workspaces",
      "placeholder": "~/Documents/Octarine, ~/Work/Notes"
    },
    {
      "name": "excludedFolders",
      "type": "textfield",
      "placeholder": "folder1, folder2, ...",
      "title": "Exclude following folders",
      "required": false,
      "description": "Specify which folders to exclude (comma separated)"
    }
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "open-workspace",
      "title": "Open Workspace",
      "description": "Open a workspace in Octarine",
      "mode": "view"
    },
    {
      "name": "search-notes",
      "title": "Search Notes",
      "description": "Search notes across Octarine workspaces",
      "mode": "view",
      "preferences": [
        {
          "name": "showWorkspaceNoteCount",
          "type": "checkbox",
          "title": "Show Workspace Note Count",
          "description": "Show or hide note counts in workspace section titles",
          "label": "Show note count in workspace section titles",
          "default": false,
          "required": false
        }
      ]
    },
    {
      "name": "open-today-note",
      "title": "Open Today's Note",
      "description": "Opens today's note in Daily Desk.",
      "mode": "view",
      "preferences": [
        {
          "name": "workspaceName",
          "type": "textfield",
          "title": "Workspace Name",
          "required": false,
          "description": "Default workspace. If empty, a selector will be shown."
        }
      ]
    },
    {
      "name": "open-daily-desk-note",
      "title": "Open Daily Desk Note",
      "description": "Open a daily or weekly note in Daily Desk by date.",
      "mode": "view",
      "arguments": [
        {
          "name": "date",
          "type": "text",
          "required": true,
          "placeholder": "feb 3"
        },
        {
          "name": "workspace",
          "type": "text",
          "required": false,
          "placeholder": "Workspace name"
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.7",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
